# Virtual env
.venv/
# Byte-compiled / cache
__pycache__/
*.py[cod]
# OS noise
.DS_Store
# Secrets / local overrides
.env
controls/*.json          # the big OSCAL catalogs (downloaded, not committed)
